# First, install with conda env
# conda install --file requirement.txt -c conda-forge -c anaconda -c benensta -c pytorch -c default
python=3.9
jax
scipy
scikit-learn
pytorch
torchvision
torchaudio
tqdm

# Second, install with pyibex with pip
# pip install pyibex
